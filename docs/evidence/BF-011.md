# BF-011 验收记录：RBAC 权限

- 后端定义了统一权限清单和四类角色矩阵。
- `RequirePermissions` 装饰器声明接口需要的权限，`PermissionsGuard` 从 JWT 角色计算权限，不接受客户端自报角色。
- 无权限返回稳定错误码 `MISSING_PERMISSION`，未登录返回 `UNAUTHORIZED`。
- 已提供演示账号：运营、仓库、分析；密码均为 `admin123`。
- 当前还没有商品、订单等真实业务接口，因此本阶段先验收权限层和守卫；BF-013 起每个业务接口将绑定具体权限，并与商户隔离守卫一起使用。
