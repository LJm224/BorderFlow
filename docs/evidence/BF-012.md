# BF-012 验收记录：前端认证壳

- 未登录访问 Web 时显示登录页，字段包括商户编码、邮箱和密码。
- 登录成功后保存短期 access token，并显示当前用户；刷新页面会使用 HttpOnly refresh cookie 恢复会话。
- 退出登录会调用后端退出接口、清除本地 access token 并回到登录页。
- 所有认证请求通过统一 API client 发送，自动携带 `/api` 前缀和 Cookie。
- Web 测试、类型检查和生产构建均已通过。
